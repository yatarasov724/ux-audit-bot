# 🎨 UX Audit Bot

Веб-приложение для анализа пользовательского опыта (UX) и технических характеристик веб-сайтов.

## 🚀 Возможности

- **UX Аудит**: Анализ пользовательского опыта
  - Визуальная иерархия
  - Навигация
  - Типографика и читаемость
  - Интерактивность
  - Мобильная адаптивность
  - Доступность

- **Lighthouse Аудит**: Технический анализ
  - Производительность
  - SEO
  - Безопасность
  - Лучшие практики

## 📋 Требования

- Node.js >= 18.0.0
- npm >= 9.0.0

## 🛠️ Установка

```bash
# Клонировать репозиторий
git clone <your-repo-url>
cd ux-audit-bot

# Установить зависимости
npm install

# Запустить локально
npm start
```

Приложение будет доступно по адресу: `http://localhost:3000`

## 🌐 Деплой

Проект готов к деплою на различных платформах. Выберите один из вариантов:

### Railway (Рекомендуется)

1. Зайдите на [railway.app](https://railway.app)
2. Войдите через GitHub
3. Создайте новый проект → "Deploy from GitHub repo"
4. Выберите ваш репозиторий
5. Railway автоматически определит настройки из `railway.json`

**Преимущества**: Простой деплой, автоматическое определение настроек, бесплатный tier

### Render

1. Зайдите на [render.com](https://render.com)
2. Создайте новый "Web Service"
3. Подключите ваш GitHub репозиторий
4. Render автоматически использует `render.yaml` для настройки

**Настройки**:
- Build Command: `npm install`
- Start Command: `node server.js`
- Environment: `Node`

**Преимущества**: Бесплатный tier, простой интерфейс

### Fly.io

1. Установите Fly CLI:
   ```bash
   curl -L https://fly.io/install.sh | sh
   ```

2. Войдите в Fly.io:
   ```bash
   fly auth login
   ```

3. Создайте приложение:
   ```bash
   fly launch
   ```

4. Деплой:
   ```bash
   fly deploy
   ```

**Преимущества**: Хорошая поддержка Docker, бесплатный tier

### Heroku

1. Установите Heroku CLI
2. Создайте приложение:
   ```bash
   heroku create your-app-name
   ```

3. Деплой:
   ```bash
   git push heroku main
   ```

**Примечание**: Heroku больше не предлагает бесплатный tier

## 🔧 Переменные окружения

При необходимости можно настроить следующие переменные:

- `PORT` - Порт для запуска сервера (по умолчанию: 3000)
- `NODE_ENV` - Окружение (production/development)

## 📝 Структура проекта

```
ux-audit-bot/
├── pages/
│   ├── api/
│   │   └── audit.js
│   └── index.js
├── public/
│   └── index.html
├── utils/
│   ├── audit.js          # Lighthouse аудит
│   └── ux-audit.js       # UX аудит
├── lang/
│   ├── en.json
│   └── ru.json
├── server.js
├── package.json
├── Dockerfile            # Для Fly.io
├── fly.toml             # Конфигурация Fly.io
├── render.yaml          # Конфигурация Render
└── railway.json         # Конфигурация Railway
```

## 🐛 Решение проблем

### Puppeteer не запускается

На некоторых платформах может потребоваться установка дополнительных зависимостей Chrome. Dockerfile уже содержит все необходимые зависимости.

### Таймауты при аудите

Если аудит занимает слишком много времени, увеличьте таймауты в `server.js`:
```javascript
req.setTimeout(180000); // 3 минуты
res.setTimeout(180000);
```

## 📄 Лицензия

MIT

## 👨‍💻 Автор

Создано с ❤️ для анализа UX веб-сайтов


